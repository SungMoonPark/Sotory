# PostgreSQL 기본 설정

listen_addresses = '*'
port = 5432

# 로그 설정 (선택)
logging_collector = on
log_directory = 'log'
log_filename = 'postgresql.log'

# 기본 커넥션 수
max_connections = 100

# 기타 필요한 설정들 (필요시 추가)